# Auth Module

Handles user authentication, authorization, and token management.

## Services

- AuthService: Core authentication logic (login, register, profile management)
- RefreshTokenService: Manages JWT refresh tokens
- PasswordResetService: Manages recovery tokens and password reset flow
- EmailChangeService: Manages two-step email change flow
